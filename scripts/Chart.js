'use strict'

class Chart extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <window.Recharts.LineChart>
        
      </window.Recharts.LineChart>
    );
  }
}